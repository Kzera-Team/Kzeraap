const fs=require('fs');function a(c,m){if(!c)throw new Error(m)};const p=fs.readFileSync('src/domain/item/ItemImportacaoParser.ts','utf8');const g=fs.readFileSync('src/infrastructure/importacao/ItemSpreadsheetImportGateway.ts','utf8');a(p.includes('parseItemImportacaoCsvTsv'),'parser');a(p.includes('detectarSeparador'),'separador');a(p.includes('parseLine'),'aspas');a(p.includes('preco'),'preco');a(!g.includes("from 'xlsx'"),'sem xlsx no build oficial');a(g.includes('temporariamente bloqueada')&&g.includes('CSV'),'orienta CSV');console.log('item-catalogo-parser.test.cjs OK');
