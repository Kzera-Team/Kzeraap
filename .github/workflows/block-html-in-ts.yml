name: Bloquear HTML em TS

on:
  pull_request:
  push:

jobs:
  no-html-in-ts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm run check:no-html-in-ts
