# MOVED — Registro bruto José GPT

Este arquivo foi criado inicialmente por engano dentro de `.claude/agents/GPT/`.

Não usar este caminho como registro bruto.

Local canônico atual:

```txt
docs/memoria/GPT/bruto.md
```

Motivo: `.claude/agents/` é área de configuração/inicialização dos agentes Claude, não local correto para histórico/memória operacional do José GPT.

Este arquivo deve ser removido fisicamente quando houver operação de delete disponível no fluxo de Git Bridge/PR.
