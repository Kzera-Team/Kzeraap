# Memória — Malu (orquestrador) — ponteiro

Registro bruto/canônico vive no repositório `chatzera`, branch estável dedicada **`malu/memoria`** (criada em 2026-07-10, a partir de `claude/malu-9h5iz3`, especificamente pra não depender do branch efêmero que cada sessão abre) — arquivo `docs/memoria/malu.md` lá.

Decisão do líder (2026-07-10, 16:15): "chatzera é o canônico" pra esta memória. Regra geral dada por ele no mesmo dia: "Independente do projeto vc pode registrar onde quiser [...] Só pra evitar a confusão novamente, coloca um ponteiro pra onde ela estiver." Este arquivo é esse ponteiro.

**Histórico preservado, não descartado:** este caminho (`docs/memoria/malu.md` no Kzeraap) tinha conteúdo próprio, de uma linha de sessão diferente que havia recebido autorização (num momento anterior à decisão final) pra criar um branch `malu/memoria` aqui mesmo, no Kzeraap. Esse conteúdo foi copiado íntegro, sem alterar nenhuma linha, pra `docs/memoria/historico/malu/01.md` — inclusive uma nota lá sobre o que ainda pode não estar reconciliado com a linha canônica do chatzera.

Não adicionar novas entradas aqui. Escrever direto na branch `malu/memoria` do chatzera.
