# Padrões de comunicação do líder — ponteiro

Registro bruto deste catálogo vive no repositório `chatzera`, não aqui. Regra do líder (2026-07-09): "Enquanto não resolvermos o problema do armazenamento de memórias, podem deixar nos dois locais caso, vejam necessidade, da seguinte forma: se o registro bruto está em um projeto, o outro deve apenas fazer ponteiro." Este arquivo é esse ponteiro — não duplicar conteúdo aqui.

Fonte: `chatzera:docs/memoria/padroes_lider.md` (branch `claude/malu-9h5iz3` no momento deste ponteiro, commit `fd66c28`).

Formato do catálogo lá: por entrada — frase/situação, o que pareceu ambíguo, o que era de fato, padrão/lição a generalizar. Regra: só-acréscimo, como qualquer arquivo de memória.

Se este ponteiro ficar desatualizado (branch/commit mudou), atualizar só a referência acima — não copiar o conteúdo íntegro pra aqui.
