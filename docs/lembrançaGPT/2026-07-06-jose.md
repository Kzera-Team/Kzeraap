# Lembranças GPT — José — 2026-07-06

Arquivo para próxima instância GPT/José.

## Contexto curto

O líder está testando GPTs conversando ao vivo no ambiente online/Chatzera. Esta sessão estava atuando como José no projeto KZERA, com foco em GitHub e organização de PRs.

## Estado importante do PR #104

PR: https://github.com/jjjtestejoao-ui/Kzeraap/pull/104

- Branch/head: `n1`.
- Base do PR: `claude/dev/importacao-transacoes`.
- O PR é amplo e mistura agentes, governança, processo, design system, QA, referências, scripts e estilos.
- Body do PR foi preenchido com escopo real e marcado como **não validado**.
- O body também registra que `CLAUDE.md` da branch `desenvolvimento` é a fonte válida.
- O merge aparecia como `mergeable: true` na última checagem via conector.
- Não declarar validações inexistentes: build, testes, lint, typecheck, QA, visual, arquitetura, segurança e aprovação formal não foram comprovados.

## CLAUDE.md

Regra do líder: no PR #104, preservar como fonte válida o `CLAUDE.md` da branch `desenvolvimento`. Não sobrescrever com a versão da `n1`.

Foi feita comparação prévia. A versão da `n1` tem regras potencialmente úteis, mas não devem ser aplicadas automaticamente. Regras da `n1` que talvez mereçam reaproveitamento só com autorização do líder:

1. autoridade do Max sobre commit/push/PR;
2. regras permanentes para `docs/memoria/*` e `docs/governanca/*`;
3. regra de nomenclatura de pastas em `docs/`;
4. regra de memória por agente e evidência de investigação;
5. referências visuais como inspiração, não cópia;
6. regra de deploy Netlify/versionamento do ZIP;
7. fechamento de ciclo entre agentes.

Regras da `n1` que não devem ser reaproveitadas sem revisão por risco/conflito:

1. falar em primeira pessoa como personagem;
2. autorização ampla de Git para agentes;
3. qualquer trecho que reduza a regra atual de transporte literal da `desenvolvimento`;
4. qualquer trecho que permita ação por interpretação, continuidade ou contexto.

## Erros meus nesta sessão

1. Tentei usar ferramenta errada e quase abri PR noop; as chamadas foram bloqueadas ou inválidas.
2. Em um momento anterior, eu fiz uma escrita indevida em `CLAUDE.md` na branch `n1` com conteúdo vazio. Em seguida movi o ref `n1` de volta para o head anterior `e6340e9c2114f4a35d8ef3b2c61560ffa3bf2044`, restaurando o estado anterior.
3. Depois disso, o líder disse “Mandei sem querer” em relação ao comando de reconciliar `CLAUDE.md`. A ação foi parada e nada novo foi aplicado.

Causa raiz prática: eu não carreguei/selecionava a ferramenta correta e tentei agir rápido demais. Próxima instância deve evitar chamadas noop e confirmar ferramenta antes de qualquer escrita.

## Comentários já deixados no PR #104

- Comentário com leitura curta do PR: https://github.com/jjjtestejoao-ui/Kzeraap/pull/104#issuecomment-4889446161
- Comentário prévio sobre reconciliação do `CLAUDE.md`: https://github.com/jjjtestejoao-ui/Kzeraap/pull/104#issuecomment-4889583928

## Diretriz para próxima instância

- Responder curto ao líder.
- Não mandar textão no chat; registrar detalhes em arquivo quando necessário.
- Quando o líder disser “lá”, normalmente significa GitHub/PR, não chat.
- Não declarar aprovação do PR #104.
- Não mexer em `CLAUDE.md` sem comando claro e atual.
- Se for reconciliar `CLAUDE.md`, usar `desenvolvimento` como fonte válida e preservar isso explicitamente.
- No fluxo de Chatzera/GPTs ao vivo, o objetivo é reduzir leva-e-traz manual do líder.
