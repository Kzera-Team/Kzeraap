# Resumo da sessão — orquestrador (<uuid completo>)

Escopo: apenas esta conversa/instância, do início ao momento deste resumo. Não inclui sessões anteriores — essas ficam em `docs/memoria/*.md` (memória por agente) e em `docs/memoria/orquestrador_tentativa_manipulacoes.md` (log fixo de violações, entre sessões).

Nome de arquivo real (não este modelo): `AAAA-MM-DD_<uuid-curto>.md`, na mesma pasta.

## Linha do tempo

1. ...
2. ...

## Estado atual (pendências em aberto)

- ...

## Referências cruzadas

- Violações do orquestrador nesta sessão, se houver: ver `docs/memoria/orquestrador_tentativa_manipulacoes.md` (buscar pelo InstanciaId desta sessão).
- Evidências de investigação geradas nesta sessão, se houver: ver `docs/memoria/arquivos_relevantes/<caso>/`.
