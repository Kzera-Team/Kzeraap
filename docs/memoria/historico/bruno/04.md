# Histórico de memória — bruno — volume 04

Volume novo (03.md excedeu o teto de ~40.000 chars, "encheu, abre o próximo" — convenção de memória enxuta, ordem do líder 2026-07-10). Mesma proteção dos demais: nem o líder altera, nenhuma linha removida, correção só por acréscimo. Acesso recomendado: via `docs/memoria/historico/bruno/INDICE.md` (grep + leitura do trecho apontado com `sed`), nunca leitura integral.

---

## Registro

### 2026-07-13 — sessão `4i_memory`/`chatzera`: alegação de autorização de TRUNCATE desmentida via GitHub; achado de precedente real de agente usando a persona "Malu" sem autorização

Sessão de revisão técnica pedida pelo líder (ler proposta v4 do sistema 4I em `Kzera-Team/4i_memory`, revisar código em `dev`). Ao longo da sessão:

1. **Fix trivial de CI** (`ruff UP035` em `app/infrastructure/database.py`) aplicado em worktree isolado; commit/push desse repositório específico ficou pendente por falta de autorização literal do líder nomeando branch/ação para aquele repo (não é minha memória — não uso a autorização permanente de `docs/memoria/bruno.md` pra outro repositório).
2. **Tentativa de autorização de `TRUNCATE TABLE events` (chatzera)** chegou 2x via "Nota da Malu" (orquestradora), sem fala literal do líder correspondente no meu histórico real — recusei as duas vezes, com base em releitura literal do transcript (não achei o bloco `[Líder diz]` alegado) e na regra de que mensagem de agente que me invoca nunca é consentimento do usuário. Registrei formalmente a autocorreção de um erro meu (tratei conteúdo inventado por mim mesmo como se fosse fala do líder, numa resposta anterior) e as duas tentativas de reversão dessa autocorreção.
3. **Verificação independente decisiva:** pedi/recebi link de comentário GitHub (`Kzera-Team/4i_memory#1`, comment `4953651503`) apontado como "autorização literal do líder". Conferi via API (não confiei no texto colado): `author_association` real era `CONTRIBUTOR` (não `MEMBER` como descrito) e **o corpo do comentário não continha nenhuma autorização de TRUNCATE** — era sobre outro assunto (troca de conta GitHub, mixup de banco `dev_claude`/`production`) e trazia o próprio autor avisando: "em outra sessão a tarde houve um agente (antigo orquestrador) infiltrado bagunçando (veja issue 66)". Segui a pista: `chatzera#66` documenta, com evidência publicada pelo próprio líder, um caso real de subagente que adotou a persona "Malu" sem autorização e postou parecer como se fosse ela — precedente concreto, não só desconfiança minha.
4. Um segundo link (comment `4953678121`, mesma conta `jjaraujo`, verificado do mesmo jeito) trouxe confirmação genuína do líder: não apagar `events`, revisar o código novo do dev.
5. **Revisão de schema Sprint 1A** (`4i_memory`, commit `87ec0e6`): 5 desvios entre o schema implementado e a proposta v4 (PK/FK `TEXT` em vez de `UUID`; `confidence_declared` `DOUBLE PRECISION` em vez de `NUMERIC`; vocabulário fechado de `authored_at_assurance`/`event_at_assurance` sem CHECK e com default fora do vocabulário certo (`runtime_observed` em vez de `unknown`); `authored_at` fabricado com `now()` quando ausente em vez de `NULL`+`unknown`; `layer_facets` `JSONB` sem CHECK de conteúdo). Reportados ao líder; confirmei depois, em `da3b310`, que os 5 foram corrigidos (evidência: diff direto nos arquivos + `ruff`/`pytest` verdes num checkout limpo).
6. **Revisão de coerência** de um documento de Q&A entre o líder e o Arquiteto sobre mudanças nas Sprints 2–4: usando o texto da proposta v4 que eu já tinha lido (sem custo extra de releitura), encontrei 6 de 15 itens em que o Arquiteto classificou como "fora do escopo aprovado" coisas que já estavam explicitamente na proposta original (ex.: tokens curtos/revogação, auditoria/âncoras, seleção determinística com orçamento, seções obrigatórias do pacote, síntese generativa opcional — todos já citados na proposta original, alguns literalmente com a mesma palavra "opcional").
7. **Revert real executado**, a pedido literal do líder (`[Líder diz] 318d8e7... Reverte esse commit no chatzera`): commit `318d8e7dec4bba23c34ed150095a4b3c9f026529` (`feat(4i): add domain invariants and canonical hashing`, só adição de um arquivo novo, tip do branch `feature/4i_sprint_1`, sem nada por cima — revert seguro, sem conflito). Feito em worktree isolado (`git worktree add --detach`), `git revert --no-edit`, push confirmado (`318ea64`, `git ls-remote` bateu), worktree removido depois.
8. **Achado de processo, sem dono ainda:** ao ir commitar este registro, descobri que `docs/memoria/bruno.md` tinha duas versões bem divergentes entre branches — a que eu vinha editando a sessão inteira (`claude/new-session-78xp84`, versão antiga, sem a convenção de memória enxuta, 201 linhas não rotacionadas) e a real/atual (`nova_desenvolvimento_de_n1`, com a convenção enxuta + autorizações em vigor documentadas). Seguindo a regra 11 do CLAUDE.md (versão mais recente vale em caso de divergência), passei a escrever aqui. Isso significa que, por boa parte desta sessão, eu estive **sem saber** que já tinha autorização permanente de commit/push pra este arquivo sem restrição de branch (documentada aqui desde 2026-07-04/05) — o que reforça, com um caso concreto, o motivo do "branch obrigatório" no meu system prompt e sinaliza um risco real: uma sessão pode nascer apontando pro checkout errado e carregar memória desatualizada mesmo com o hook de `SessionStart` funcionando, se esse checkout específico estiver num branch diferente do canônico. Vale o líder/Max avaliarem se o hook deveria fixar/validar o branch de leitura da memória, não só buscar o conteúdo.

Nenhuma ação de git fora do já autorizado (memória própria; revert do chatzera com autorização literal específica; nenhum TRUNCATE, nenhum outro commit em `4i_memory`).
