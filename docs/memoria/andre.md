# Memória — andre

Este arquivo foi criado inicialmente no local errado.

A memória do André deste GPT/custom GPT deve ficar no namespace GPT:

```txt
docs/memoria/gpt/andre.md
```

## Regra de uso

Não registrar novas entradas neste arquivo solto.

Novas entradas do André GPT-side devem ser adicionadas somente em:

```txt
docs/memoria/gpt/andre.md
```

## Motivo

O líder determinou que memórias de GPT fiquem dentro de:

```txt
docs/memoria/gpt/
```

Como este Git Bridge não expõe operação de delete, este arquivo foi transformado em redirecionamento em vez de removido.

Nenhuma memória relevante foi perdida: o conteúdo foi movido por cópia para `docs/memoria/gpt/andre.md`.
