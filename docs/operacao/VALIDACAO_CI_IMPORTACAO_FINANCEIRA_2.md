# PR de validação CI

Este arquivo complementa a validação DevOps para acionar o workflow corrigido contra `desenvolvimento`.

Resultado esperado:

- Build não deve falhar com `vite: not found`.
- CI deve avançar para testes/typecheck.
